import tokens from './tokens.json';

export const palette = {
  primary: tokens.tokens.paletteLight.primary,
  secondary: tokens.tokens.paletteLight.secondary,
  error: tokens.tokens.paletteLight.error,
  background: {
    default: tokens.tokens.paletteLight.background.Paper,
  },
  text: tokens.tokens.paletteLight.text,
};
